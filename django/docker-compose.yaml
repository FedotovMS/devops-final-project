version: "3.9"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      DB_HOST: localhost
      DB_NAME: testdb
      DB_USER: test
      DB_PASSWORD: test
      DB_PORT: 5432